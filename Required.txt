{
  "goal": "Fix all runtime errors, API mismatches, null issues, and state problems in Flutter turf booking app",

  "flutter_fixes": [
    "ensure all API responses are null-safe",
    "add null checks before using response data",
    "handle empty API responses",
    "add try-catch for all async calls",
    "show loading indicator during API calls",
    "avoid calling setState after dispose"
  ],

  "api_response_fix": {
    "rule": "standardize API response",
    "format": {
      "success": true,
      "data": []
    },
    "ensure": [
      "response contains data field",
      "data is always an array",
      "no undefined fields"
    ]
  },

  "slot_fetch_fix": {
    "issues": [
      "timeSlot field missing",
      "wrong mapping logic"
    ],
    "fix": [
      "ensure booking schema has timeSlot",
      "validate response before mapping",
      "use safe mapping: bookings?.map(...) ?? []"
    ]
  },

  "null_safety": [
    "check widget.turf != null",
    "check widget.turf.id != null",
    "check selectedDate != null",
    "use optional chaining everywhere"
  ],

  "state_management": [
    "set loading true before API",
    "set loading false after API",
    "update UI only if mounted == true"
  ],

  "backend_fixes": [
    "ensure /bookings API returns correct data",
    "include timeSlot in booking response",
    "filter bookings by turfId and date"
  ],

  "debugging": [
    "log API response",
    "log request params",
    "print errors clearly"
  ],

  "fallback_ui": [
    "if no slots → show 'No slots available'",
    "if error → show retry button"
  ],

  "final_result": [
    "no red errors",
    "slots load properly",
    "UI stable",
    "no crashes"
  ]
}
