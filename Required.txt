{
  "project": "Turf Booking App - Safe Full Build with Existing Database",

  "goal": "Build a complete turf booking application using existing MongoDB Atlas database WITHOUT disconnecting, modifying, or deleting existing data",

  "critical_rules": {
    "do_not_disconnect_database": true,
    "use_existing_connection_string": true,
    "do_not_delete_or_drop_collections": true,
    "do_not_modify_existing_documents": true,
    "only_add_new_fields_if_required": true,
    "system_must_work_with_existing_data": true
  },

  "database_strategy": {
    "mode": "read_and_extend",
    "rules": [
      "reuse existing collections",
      "add optional fields only",
      "create new collections for new features",
      "no schema breaking changes"
    ]
  },

  "existing_collections_support": [
    "users",
    "admins",
    "turfs",
    "bookings"
  ],

  "new_collections_to_create": [
    "payments",
    "reviews",
    "notifications",
    "amenities",
    "turf_amenities",
    "pricing_rules"
  ],

  "multi_admin_system": {
    "requirement": "data isolation per admin",
    "implementation": [
      "ensure turf has owner_id",
      "filter all turfs by owner_id",
      "filter bookings by owner_id",
      "filter payments by owner_id"
    ]
  },

  "search_filter_system": {
    "filters": [
      "district",
      "place",
      "price_min",
      "price_max"
    ],
    "requirement": "dynamic filtering without affecting existing queries"
  },

  "commission_model": {
    "percentage": 5,
    "rules": [
      "do not modify stored turf price",
      "calculate commission at runtime",
      "store final_price separately"
    ]
  },

  "payment_system": {
    "gateway": "Razorpay",
    "rules": [
      "use test mode first",
      "do not block booking if payment fails",
      "store payment details separately"
    ]
  },

  "backend_requirements": {
    "architecture": "modular (routes, controllers, services, models)",
    "features": [
      "JWT authentication",
      "role-based admin access",
      "secure APIs",
      "error handling",
      "logging"
    ]
  },

  "frontend_requirements": {
    "rules": [
      "do not break existing UI",
      "add new components only",
      "hide feature if API fails"
    ],
    "features": [
      "search by district and place",
      "price filter",
      "turf details with amenities",
      "reviews UI",
      "payment screen",
      "admin dashboard",
      "logout button"
    ]
  },

  "logout_system": {
    "implementation": [
      "clear JWT token",
      "invalidate session",
      "redirect to login screen"
    ]
  },

  "safety_features": {
    "fallback_mode": true,
    "feature_flags": true,
    "non_blocking_errors": true
  },

  "deployment": {
    "safe_deploy": true,
    "no_downtime": true,
    "use_env_variables": true
  },

  "expected_output": [
    "full backend code",
    "full frontend UI",
    "mongodb integration",
    "safe data handling",
    "filter system",
    "admin isolation",
    "payment integration",
    "setup guide"
  ]
}
