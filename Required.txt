Role: Act as a Senior Full-Stack Developer.
Project: Turf Slot Booking Mobile App (React Native + Node.js).
​Task: Generate a production-ready starter code for a Turf Booking System with a high-end UI.
​1. Backend (Node.js & MongoDB):
​Create a server.js with Express.
​Define Mongoose Schemas for:
​User: (Name, Email, Phone, Password).
​Turf: (Name, Location, PricePerHour, ImageURL).
​Booking: (User ID, Turf ID, Date, TimeSlot, Status).
​Create an API endpoint POST /book that checks if a slot is already booked for that date and turf before saving.
​Create an API endpoint GET /slots/:turfId/:date to return available time slots.
​2. Frontend (React Native - Expo):
​Create a modern HomeScreen using FlatList to show available turfs in cards.
​Create a BookingScreen with:
​A Date Picker (or Calendar).
​A Time Slot Grid (e.g., 6 AM to 11 PM) where booked slots are disabled/red and available slots are clickable/green.
​Use Axios for API calls.
​3. Style Preference:
​Use a "Midnight Sports" theme: Dark background (#121212), Neon Green accents (#39FF14), and White text.
​4. Output: > - Provide the code for server.js, models.js, and the main React Native components.
​List all npm install commands for both backend and frontend.
​Explain how to run both simultaneously.
